# Python
__pycache__/
*.py[cod]
*$py.class
*.log

# Environment & Temp
.env
.DS_Store
Thumbs.db

# 비밀키 / 자격증명 (절대 커밋 금지)
*.pem
gemini_key.txt

# 런타임 생성 데이터
push_subscriptions.json

# 서버가 5초마다 쌓는 시간대별 혼잡도 관측 기록 (매주 월요일 갱신)
congestion_stats.json
data/

# AI 생성 스크래치 출력물
gemini_output.txt
gpt_120b_reply.txt
reply_*.txt

# 봇 배치도 렌더 테스트 산출물
bot_preview.png

# 봇 런타임 데이터
bot_settings.json
discord_alarms.json
# 기기 오류/일시정지 이력 (일주일치, 서버에만 둔다)
device_events.json

# =========================================================
# 기관 내부 자료 — 절대 커밋하지 않는다
# ---------------------------------------------------------
# 안내 지식과 캠퍼스 사진은 코드가 아니라 기관의 자료다.
# 공개 저장소에 한 번 올라가면 되돌릴 수 없다.
# 본보기는 jungle_kb.example.py 를 참고한다.
# =========================================================
jungle_kb.py
jungle_kb.js
assets/*.webp
assets/*.png
assets/*.jpg
assets/README.md

# 식당 카카오 채널에서 가져온 식단 (서버에만 둔다)
cafeteria.json
cafeteria_weekly.cache
